{
  "name": "js-ts-stripe",
  "version": "1.0.0",
  "description": "TypeScript, Node, Stripe Payment Gateway",
  "main": "src/index.ts",
  "scripts": {
    "build": "swc src --out-dir dist",
    "start": "node --env-file=.env dist/src/index.js",
    "dev": "nodemon --watch src --ext ts --exec 'npm run build && npm start'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "devDependencies": {
    "@swc/cli": "^0.3.12",
    "@swc/core": "^1.5.5",
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.11",
    "chokidar": "^3.6.0",
    "nodemon": "^3.1.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
